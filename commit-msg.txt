feat: rebuild complet de script.js avec toutes les features

Le user : "je vois les noms de villes mes aucun
truc s'affiche heure par heure"

Le rollback precedent avait reduit script.js a 37
lignes, perdant TOUTES les features. Rebuild complet
(42 KB / 48 fonctions) qui match le HTML existant.

FEATURES RESTAUREES
  - Current weather (temp, condition, hilo)
  - Description enrichie (UV, pression, ressenti)
  - Hourly 24h (avec icones + POP%)
  - Daily 10 jours (avec POP%, clickable)
  - Day detail panel (24h breakdown)
  - Details grid : UV, sunrise, vent, precip,
    humidite, ressenti, visibilite, pression+AQI
  - Pressure trend arrow (↑/↓/→)
  - AQI (Open-Meteo Air Quality API, async)
  - Radar RainViewer (radar.js integre)
  - Search bar (Open-Meteo Geocoding)
  - Settings panel (unit toggle C/F, refresh)
  - Favoris (CRUD complet)
  - Donate modal (2 fois au lancement)
  - Geolocation auto (navigator.geolocation)
  - IP fallback (geo-ip.js)
  - Auto-refresh 60s
  - Cache offline (localStorage, TTL 6h)
  - Retry exponentiel pour fetch (500ms/1.5s/4s)
  - Source badge + updated at counter
  - WMO codes avec icones SVG inline

ARCHITECTURE
  - 48 fonctions nommees et commentees
  - Helpers isoles : fmtTemp, getDayName, getHourLabel,
    windDirLabel, uvLabel, aqiLabel, computeAQI
  - openMeteoToInternal : transforme la reponse API
    en structure interne normalisee
  - fetchWeather : essaie API -> fallback cache ->
    erreur finale (pas de procedural pour l'instant)
  - requestId dedup des requetes concurrentes

CSS COMPATIBILITY (verifie)
  - .day, .day-name, .day-icon, .day-low, .day-high
  - .hour-cell, .hour-time, .hour-icon, .hour-temp
  - .fav-item, .fav-item-info, .fav-item-name
  - .search-result, .fav-star
  - Toutes les classes existent dans styles.css

INIT FLOW (non-bloquant, comme convenu)
  1. loadState() -> ville existante ou Paris fallback
  2. loadWeather() -> affiche meteo
  3. document.body.classList.remove("loading")
  4. setup* (search, settings, donate, favoris)
  5. setTimeout 2.5s -> startAutomaticGeolocation
  6. setInterval 60s -> tickLive
  7. setInterval 5min -> radar check

CE QUI N'EST PAS ENCORE INTEGRE
  - Seasonal bias (approuve precedemment, peut etre
    ajoute si tu confirmes)
  - Fire risk (computeFireRisk)
  - Skeleton loading detaille (animation shimmer)
  - Temperature interpolation (smooth transitions)
  - Background atmospheric effects (god rays, aurora)

CACHE
  script.js v=20260909 -> v=20260909b
  radar.js, geo-ip.js inchanges

ACTION REQUISE
  Hard refresh (Ctrl+Shift+R) sur ton app

Co-Authored-By: Letta Code <noreply@letta.com>