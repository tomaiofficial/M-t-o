feat: design beaucoup plus realiste (effets atmospheriques)

Le user : "Ameliore encore plus la meteo design ect plus
realiste sans rien toucher aux icones meteo"

NOUVEAUTES VISUELLES

1. VIGNETTE ATMOSPHERIQUE (.bg-vignette)
   - Coins sombres (ellipses radiales haut + bas)
   - Donne de la profondeur, atmosphere "regard"
   - Plus prononcee la nuit

2. GOD RAYS / RAYONS SOLAIRES (.bg-godrays)
   - Conic gradient filtre + blur 40px
   - Animation swing 30s alternate
   - Visible en theme-day-clear, partly-cloudy, sunset

3. AURORA / AURORES BOREALES (.bg-aurora)
   - 3 ellipses degradees vert/bleu/violet
   - Flow animation 25s
   - Visible la nuit (.theme-night-clear, opacite 0.5)

4. STARS TWINKLING (.bg-stars)
   - 12 etoiles positionnees a la main
   - Twinkle 4s alternate
   - Plus prononce la nuit claire

5. HEAT SHIMMER (.bg-heat)
   - Bandes repeat-lineaires simulent la chaleur
   - Shimmer animation 3s
   - Active si temp >= 30C (.theme-hot)

6. BREATH FOG (.bg-breath)
   - Ellipses de brume flottante
   - Fog drift 8s alternate
   - Active si temp <= 5C (.theme-cold)

7. GLASS CARDS AMELIOREES
   - Background plus subtil (rgba 0.08 vs 0.1)
   - 4 couches de shadow (exterieur + interieur
     haut + interieur bas)
   - Pseudo-elem ::before : lueur traversante haute
   - Pseudo-elem ::after : reflet de bord (1px line)
   - Position: relative + overflow: hidden

8. TYPOGRAPHIE PREMIUM
   - city-name : font-weight 500 -> 600,
     background-clip text (degrade blanc -> blanc 92%)
   - temp : font-size 96 -> 100, font-weight 200 -> 100
     (plus fin), background-clip text degrade,
     drop-shadow subtil 30px
   - condition : text-shadow renforce (12px)
   - hilo : letter-spacing 0.3px

DETAILS TECHNIQUES

- Aucune icone meteo modifiee (respect de la
  consigne utilisateur)
- Tous les effets utilisent CSS pur (pas de JS
  canvas supplementaire)
- Animations GPU-friendly (transform/opacity)
- Transitions douces 1.5s entre themes

LOGIQUE TEMPERATURE
  function tempExtraTheme(tempC)
    temp >= 30C : +"theme-hot" -> chaleur visible
    temp <= 5C  : +"theme-cold" -> buffee visible
  -> appele dans les 3 endroits ou app.className
     est defini

CACHE
v=20260820b -> v=20260823

Co-Authored-By: Letta Code <noreply@letta.com>