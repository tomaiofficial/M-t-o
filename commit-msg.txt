fix: incendies sans cle API via proxy CORS auto

Le user : "je veut sans api" — la cle MAP_KEY etait
dans la copie du panneau mais surtout NASA FIRMS
est bloque en CORS depuis un navigateur.

Changements :
1. fetchOneCsv() tente maintenant 4 strategies :
   a) Requete directe NASA FIRMS (marche parfois)
   b) corsproxy.io (proxy CORS gratuit)
   c) api.allorigins.win/raw (fallback)
   d) api.codetabs.com/v1/proxy (fallback final)
   Au moins une devrait fonctionner.

2. La section "Cle NASA FIRMS" dans le panneau est
   desormais repliee par defaut (<details>).
   Libelle clarifie : "(avance, optionnelle)".
   Sans cle : on passe par le proxy CORS public.

3. Le user peut toujours saisir une cle pour un acces
   direct (plus rapide, sans proxy) en ouvrant la
   section avancee.

Aucune autre fonctionnalite modifiee.

Cache bump : v=20260726 -> v=20260726b

Co-Authored-By: Letta Code <noreply@letta.com>