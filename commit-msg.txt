fix: app restauree + 2 bugs critiques corriges

User a rollback script.js a un mini-script (37 lignes)
qui cherche #city alors que le HTML a #cityName ->
setText("city", ...) ne fait rien -> reste fige sur
"Localisation..." (defaut HTML).

RECONSTITUTION
  - script.js complet (8.5 KB) qui match le HTML existant
  - WMO codes + loadWeatherForCoordinates corrects
  - Init flow non-bloquant (Paris d'abord, IP/GPS en
    upgrade arriere-plan)
  - Auto-refresh 60s
  - localStorage save/load (state.city persist entre
    sessions)
  - requestId dedup des requetes concurrentes
  - Gestion erreur : cityName = state.city.name si
    fetch fail

BUG #1 FIX : geo-ip.js lat/lon en STRINGS
  L'API geo.kamero.ai renvoie latitude/longitude en
  strings ("40.7126"). Mon ancienne validation
  typeof !== "number" rejetait tout.
  -> parseFloat() avant validation.

BUG #2 FIX : init flow non-bloquant
  Avant : await tryIpGeolocation() -> await loadWeather
  Si IP hang, l'app etait figee 4-8s avant d'afficher.
  Apres : Paris immediat, IP/GPS en .then() background.

  Maintenant, meme si TOUTES les sources externes
  sont KO, Paris s'affiche immediatement.

CACHE
  script.js, geo-ip.js, radar.js -> v=20260909

Ce qui n'est PAS restaure (le user a rollback):
  - Hourly forecast UI (24h)
  - Daily forecast UI (10 jours)
  - AQI (Air Quality)
  - Description IA / Pressure trend
  - Radar RainViewer (radar.js charge mais pas appele)
  - Seasonal bias
  - Settings panel
  - Favoris
  - Geolocation banner / toast
  - PWA features (skeleton, smooth temp, etc.)

Si tu veux que je restaure les features, dis-le moi
et je rebuild par-dessus cette base stable.

Co-Authored-By: Letta Code <noreply@letta.com>