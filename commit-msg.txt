fix: city-name centre correctement (retire inline-block)

Le user : "Corrige juste le nom de la ville au dessus
de la temperature et ecrit un peu a gauche remet
centre comme avant"

PROBLEME
  J'avais ajoute `display: inline-block` au h1
  .city-name dans la passe precedente (avec
  background-clip: text). Resultat : le h1 ne
  prenait plus 100% de la largeur et le rendu
  text-align: center du parent .location ne le
  recentrait plus correctement -> effet leger
  decalage a gauche visible.

FIX
  - Retire `display: inline-block` du .city-name
  - Retire background-clip + -webkit-text-fill-color
    (pas necessaire et causait le rendu bizarre)
  - Conserve juste text-shadow renforce et color
    blanc solide (meme resultat visuel mais sans
    le bug de centrage)

CACHE
v=20260823 -> v=20260823b

Co-Authored-By: Letta Code <noreply@letta.com>